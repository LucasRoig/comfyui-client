{
  "name": "comfyui-client",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=!./design-system/*",
    "dev": "turbo run dev --filter=!./design-system/*",
    "lint": "pnpm biome ci",
    "fix": "pnpm biome check --write",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "coverage": "turbo run coverage",
    "knip": "knip"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^24.6.2",
    "knip": "^5.64.1",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": ">=22"
  }
}
